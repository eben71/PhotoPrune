pre-commit:
  parallel: true
  commands:
    block-sensitive-paths:
      run: |
        scripts/block-sensitive-paths.sh
    web-format:
      run: |
        pnpm --filter web format:check
    api-format:
      run: |
        cd apps/api && uv run black --check app tests
    worker-format:
      run: |
        cd apps/worker && uv run black --check app tests
    python-lock-pins:
      run: |
        python3 scripts/check-python-lock-pins.py
    python-locks-locked:
      run: |
        cd apps/api && uv lock --locked
        cd ../worker && uv lock --locked
